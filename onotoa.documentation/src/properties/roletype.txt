=== Role Type ===

A role type is used to specify if a topic type is used to specify a role. Every topic which is used as role has to be of the type
role type.

A role type inherits the properties  of topic type (see <<_topic_type>>).


.Property Page of role type
image::images/roletype_pp.png[scaledwidth="100%"]

